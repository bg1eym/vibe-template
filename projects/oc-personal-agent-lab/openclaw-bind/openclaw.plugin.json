{
  "id": "oc-bind",
  "name": "OC Bind",
  "description": "Trigger oc-personal-agent-lab pipeline when /bind is sent. Requires OC_BIND_ENABLED=1.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean" },
      "cwd": { "type": "string", "description": "Path to oc-personal-agent-lab" }
    }
  }
}
