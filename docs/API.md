# API

Success response:
{ "success": true, "data": {} }

Error response:
{ "success": false, "error": { "code": "ERROR_CODE", "message": "message" } }

Endpoints:
- GET /health
- GET /items
- POST /items
- GET /items/:id
- PUT /items/:id
- DELETE /items/:id
