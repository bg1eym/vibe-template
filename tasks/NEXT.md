# NEXT
- [x] Add pagination to GET /items (limit/offset, defaults, max limit) + tests
- [x] Add consistent response helpers (ok/error) shared across routes
- [ ] Add config module (PORT/HOST/DB_FILE) + tests (if needed)
- [x] Add simple auth placeholder (replace x-owner-id with token stub) + tests
- [ ] Revisit npm audit: upgrade vitest/vite/esbuild chain after tests stabilize
